import { PagenationParameters } from './base/pagenation';
export interface TaskAssignment {
    id: number;
    task: object;
    is_active: boolean;
    billable: boolean;
    hourly_rate: number;
    budget: number;
    created_at: string;
    updated_at: string;
}
export interface TaskAssignmentsPagenationResponse extends PagenationParameters {
    taskAssignments: TaskAssignment[];
}
export interface TaskAssignmentsPagenationParameters extends PagenationParameters {
    is_active: boolean;
    updated_since: string;
}
