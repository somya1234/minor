import Harvest from '../index';
import { ClientsPagenationParameters, Client } from '../models/clients.models';
export declare class ClientsAPI {
    harvest: Harvest;
    baseUrl: string;
    constructor(harvest: Harvest);
    get(id: number): Promise<{}>;
    list(query?: ClientsPagenationParameters): Promise<{}>;
    create(client: Client): Promise<{}>;
    update(id: number, data: any): Promise<{}>;
    delete(id: number): Promise<{}>;
}
