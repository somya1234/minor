"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class TaskAssignmentsAPI {
    constructor(harvest) {
        this.baseUrl = '/v2/projects';
        this.harvest = harvest;
    }
    list(projectId, query) {
        return this.harvest.request('GET', `${this.baseUrl}/${projectId}/task_assignments`, query);
    }
    create(projectId, data) {
        return this.harvest.request('POST', `${this.baseUrl}/${projectId}/task_assignments`, data);
    }
    get(projectId, id) {
        return this.harvest.request('GET', `${this.baseUrl}/${projectId}/task_assignments/${id}`);
    }
    update(projectId, id, data) {
        return this.harvest.request('PATCH', `${this.baseUrl}/${projectId}/task_assignments/${id}`, data);
    }
    delete(projectId, id) {
        return this.harvest.request('DELETE', `${this.baseUrl}/${projectId}/task_assignments/${id}`);
    }
}
exports.TaskAssignmentsAPI = TaskAssignmentsAPI;
//# sourceMappingURL=taskAssignments.js.map